# LifePack

Five everyday browser tools in one purchase: finance tracker plus four simple apps. Pay once — no subscription.

- **Site:** open `microtools/index.html` locally, or deploy the `microtools` folder to any static host.
- **Checkout:** Stripe link is set in `microtools/bundle.html`.
